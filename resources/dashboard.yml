# Databricks Lakeview Dashboard configuration
resources:
  dashboards:
    contract_dashboard:
      display_name: "Contract Analysis Dashboard - ${bundle.target}"
      warehouse_id: ${var.warehouse_id}
      file_path: ../dashboard/dashboard_contract.lvdash.json
      embed_credentials: true

